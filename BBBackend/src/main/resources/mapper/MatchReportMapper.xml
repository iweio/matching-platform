<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matching.platform.mapper.MatchReportMapper">

    <select id="findByMatchId" resultType="com.matching.platform.entity.MatchReport">
        SELECT * FROM match_report WHERE match_id = #{matchId}
    </select>

    <insert id="upsert">
        INSERT INTO match_report (match_id, score, dimensions, advantage, risk, suggest)
        VALUES (#{matchId}, #{score}, #{dimensions}, #{advantage}, #{risk}, #{suggest})
        ON DUPLICATE KEY UPDATE
            score = VALUES(score),
            dimensions = VALUES(dimensions),
            advantage = VALUES(advantage),
            risk = VALUES(risk),
            suggest = VALUES(suggest)
    </insert>
</mapper>
